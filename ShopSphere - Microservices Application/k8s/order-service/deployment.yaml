apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: shopsphere
  labels: { app: order-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: order-service }
  template:
    metadata:
      labels: { app: order-service }
    spec:
      containers:
        - name: order-service
          image: shopsphere/order-service:latest
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 3003 }]
          envFrom:
            - configMapRef: { name: shopsphere-config }
            - secretRef:    { name: shopsphere-secrets }
          env: [{ name: PORT, value: "3003" }]
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          readinessProbe:
            httpGet: { path: /health, port: 3003 }
          livenessProbe:
            httpGet: { path: /health, port: 3003 }
---
apiVersion: v1
kind: Service
metadata: { name: order-service, namespace: shopsphere, labels: { app: order-service } }
spec:
  selector: { app: order-service }
  ports: [{ port: 3003, targetPort: 3003 }]
